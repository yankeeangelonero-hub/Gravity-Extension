# Handoff

Timestamp: 2026-04-02 00:26:36 +08:00
Branch: `codex-v13-state-delta`

## Current Snapshot

- Active prose architecture is the modular v14 stack: preset-owned prose kernel and dossier layers plus mode-specific World Info prose entries.
- Live setup/runtime story framing has been slimmed down. The extension no longer authors story-type, tone/style, guidelines, length, motivation, objective, or world-level knowledge asymmetry.
- `world.constants.combat_rules` is the only live setup-authored world constant still surfaced in prompt state and UI.
- Good-turn exemplar tagging now preserves the actual completed mode via `_lastCompletedMode`.

## Documentation Changes In This Handoff

- Restored active durable memory at `Documentation/project_memory.md`.
- Left archived notes in `Documentation/Old/`.
- Updated `AGENTS.md` so future sessions know the current memory path, the trimmed runtime-state contract, and the current asymmetry/blindspot status.

## Open Design Notes

- `world.knowledge_asymmetry` is not a live first-class mechanic anymore.
- Asymmetry is currently modeled indirectly through `reads`, `noticed_details`, summaries, and collisions.
- There is no `blindspots` field in the current schema.
- `pc.knowledge_gaps` is still mentioned in `ooc-handler.js` during `OOC: eval`, but it is not otherwise surfaced. It should either be implemented properly or removed.

## Suggested Next Steps

- Decide whether `pc.knowledge_gaps` should become a real tracked field.
- If explicit blindspots are added later, keep them optional and scoped to important recurring characters instead of all characters.
- Consider a cleanup pass for documentation/source-file mojibake if doc work continues.
